<%= form_for(@energy_type) do |f| %>
  <% if @energy_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@energy_type.errors.count, "error") %> prohibited this energy_type from being saved:</h2>

      <ul>
      <% @energy_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
