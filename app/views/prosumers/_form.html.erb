<%= form_for(@prosumer) do |f| %>
  <% if @prosumer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prosumer.errors.count, "error") %> prohibited this prosumer from being saved:</h2>

      <ul>
      <% @prosumer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :cluster_id %><br>
    <%= collection_select(:prosumer, :cluster_id, Cluster.all, :id, :name, :include_blank => true) %>
  </div>
    <div class="field">
    <%= f.label :users %><br>
    <%= select_tag "users", 
            options_from_collection_for_select(User.all, 
                'id', 'email', @prosumer.users.map{ |j| j.id }), 
                :multiple => true, :prompt => "Please select" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
